{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "comment": "Cron به GitHub Actions منتقل شد (.github/workflows/reminder-cron.yml) — پلن Hobby فاصلهی کوتاهتر از روزانه را اجازه نمیدهد. مسیر تریگر: POST /api/cron/reminders با Bearer CRON_SECRET.",
  "crons": []
}
